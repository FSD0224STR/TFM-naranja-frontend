const URL = "http://localhost:3000";

export const getResults = (filter) => {
  return []; //devuelve resultados que coincidan con lo escrito en el input
};

export const getFamilyResults = (productName, filter) => {
  //text2 es opcional
  return []; //return array de resultados de la misma familia
};
